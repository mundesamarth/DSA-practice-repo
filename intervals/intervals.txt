# Looping through intervals


for (let interval of intervals) {
    console.log(interval);
}

for (let [start, end] of intervals) {
    console.log(start, end);
}


#Sorting intervals

intervals.sort((a, b) => a[0] - b[0]); -> sorting by start
intervals.sort((a,b) => a[1] - b[1]); -> sorting by end

[
    [8,10],
    [1,3],
    [15,18],
    [2,6]
] 
becomes
[
    [1,3],
    [2,6],
    [8,10],
    [15,18]
]



# How to find overlapping intervals
current = [1,3]
next    = [2,6]

if (next[0] <= current[1]) {
    // overlap
}


#Finding non-overlapping intervals
current = [1,3]
next    = [8,10]

if(next[0]>current[1])


# Merging overlapping intervals
[1,4]
[2,6]

Math.min(1,2) 
Math.max(4,6)


SORT
 ↓
Keep current interval
 ↓
Look at next interval
 ↓
Do they overlap?
 /           \
YES           NO
 |             |
merge       save current
               |
           current = next



           // Access
interval[0]                 // start
interval[1]                 // end

// Destructure
let [start, end] = interval;

// Sort by start
intervals.sort((a,b) => a[0] - b[0]);

// Sort by end
intervals.sort((a,b) => a[1] - b[1]);

// Add interval
result.push([start, end]);

// Last interval
result[result.length - 1];

// Compare
nextStart <= currentEnd     // overlap
nextStart > currentEnd      // no overlap

// Merge end
Math.max(currentEnd, nextEnd);

// Number of intervals
intervals.length;